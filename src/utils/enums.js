const ROLES = require('../constants/roles');
const GAME_CONSTANTS = require('../constants/game.constants');

module.exports = {
  ROLES,
  GAME_STATES: GAME_CONSTANTS.GAME_STATES,
  CATEGORIES: GAME_CONSTANTS.CATEGORIES,
};

